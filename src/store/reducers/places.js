const initialStae = {
    places: [],
    selectedPlace: null
};

const reducer = (state = initialStae, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default reducer;
